#Containers

#Single Value Container
#Create Statement -> Creating a Single Value Container in RAM
# user_name is a reference variable
user_name = "navkiran"

# promoCode (Camel case)
# promo_code (Snake Case)
#In Python we usually use Snake Case

# Read Statement -> To read data inside container
print(user_name, id(user_name), type(user_name))
#id would represent address (address is known as hash code)

#Update Statement
user_name = "mayank"
print(user_name, id(user_name), type(user_name))
#type would represent class of SVC

#Delete Statement
#del user_name
#print(user_name, id(user_name), type(user_name))

